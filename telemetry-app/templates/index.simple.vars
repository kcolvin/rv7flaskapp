<h1> index.html </h1>

<h4>{{ts}}</h4>

{% for key, value in data.items() %}
    {{ key, value }}<p> 
{% endfor %}

const source = new EventSource("<URL>");

    source.onmessage = function (event) {
       # use event parameter
    }